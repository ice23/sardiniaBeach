$('#search').live('pageshow', function (event, ui) {
    //alert('search');
//    $(document).ready(function () {

//        $("a[id^='test']").click(function () {
//            //alert('this.val()');
//            var id = $(this).attr('name');
//            var name = $(this).attr('id');
//            // alert('id' + id + name);
//            window.location.href = "#Beach?id=" + id;
//            // $.mobile.changePage("#Beach?id=" + id);
//        });


//    });

    //    $('#searchField').on('inputchange', function () {
    //       // alert(this.value);

    //        var text = this.value;
    //        if (text.length < 1) {
    //            sugList.html("");
    //            sugList.listview("refresh");
    //        } else {

    //            //alert(sites.length + " " + text);
    //            var str = '';
    //            for (var i = 0; i < sites.length; i++) {
    //                var info = sites[i];
    //                var nome = info[0];
    //                var id = info[8];
    //                //alert(i + ' '  + nome);
    //                if (nome.toUpperCase().indexOf(text.toUpperCase()) != -1) {
    //                    //alert(info[0]);
    //                    str += "<li><a href=#Beach?id=" + id + ">" + info[0] + "</a></li>";
    //                }
    //            }
    //            // alert(str);
    //            sugList.html(str);
    //            sugList.listview("refresh");
    //        }
    //    });


    //    $("#searchField").change(function(){ 
    //        var text = $(this).val();
    //        if (text.length < 1) {
    //            sugList.html("");
    //            sugList.listview("refresh");
    //        } else {

    //            //alert(sites.length + " " + text);
    //            var str = '';
    //            for (var i = 0; i < sites.length; i++) {
    //                var info = sites[i];
    //                var nome = info[0];
    //                var id=info[8];
    //                //alert(i + ' '  + nome);
    //                if (nome.toUpperCase().indexOf(text.toUpperCase()) != -1) {
    //                    //alert(info[0]);
    //                    str += "<li><a href=#Beach?id=" + id + ">" + info[0] + "</a></li>";
    //                }
    //            }
    //            // alert(str);
    //            sugList.html(str);
    //            sugList.listview("refresh");
    //        }
    //    });


});
